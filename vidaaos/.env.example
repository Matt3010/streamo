# Optional build-time defaults. The TMDB key can also be entered at runtime in
# Impostazioni > Catalogo. Leave VITE_PROXY_ORIGIN empty when /prov and /vix are
# served by the same origin as the app.
VITE_TMDB_API_KEY=
VITE_PROXY_ORIGIN=
